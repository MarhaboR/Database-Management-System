-- creating Book table (Book_id, Title, Publisher_Name, Pub_Year) with Book_id being a primary key)
CREATE TABLE BOOK (
  Book-id INT (20),
  Title CHAR(20),
  Publisher_Name CHAR(50),
  Publisher_Year INT(20),
  PRIMARY KEY (Book-id)
  
);
